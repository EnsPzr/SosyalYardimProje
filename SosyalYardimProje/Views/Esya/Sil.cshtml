@model BusinessLayer.Models.EsyaModelleri.EsyaModel

@{
    ViewBag.Title = "Sil";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Eşya Sil</h2>

<h3>Silmek İstediğinize Emin Misiniz? Eşya ile Bağlantılı Tüm Veriler Silinebilir.</h3>
<div>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.EsyaAdi)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.EsyaAdi)
        </dd>

    </dl>

    @using (Html.BeginForm("EsyaSil", "Esya", FormMethod.Post))
    {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            @Html.Hidden("id", Model.EsyaId)
            <input type="submit" value="Sil" class="btn btn-danger" /> |
            @Html.ActionLink("Listeye Geri Dön", "Liste")
        </div>
    }
</div>
